[pytest]
markers =
    unit: isolated logic tests
    integration: multi-component tests
    slow: bounded stress and long-running tests
    release: release manifest and bundle checks
    headless: SDL dummy-driver smoke tests
